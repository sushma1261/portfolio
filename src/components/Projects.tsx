import { Box, Typography } from "@mui/material";

const Projects = () => {
  return (
    <Box>
      <Typography
        variant="h4"
        color="white"
        style={{ textShadow: "1px 1px 2px black" }}
      >
        Projects
      </Typography>
    </Box>
  );
};

export default Projects;
